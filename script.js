const menuBtn=document.getElementById('menuBtn');const mobileNav=document.getElementById('mobileNav');if(menuBtn){menuBtn.addEventListener('click',()=>{const open=mobileNav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open));menuBtn.textContent=open?'Close':'Menu'});mobileNav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{mobileNav.classList.remove('open');menuBtn.setAttribute('aria-expanded','false');menuBtn.textContent='Menu'}))}const cards=[...document.querySelectorAll('.journey-card')];const fill=document.getElementById('progressFill');const label=document.getElementById('progressLabel');const observer=new IntersectionObserver(entries=>{const visible=entries.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(!visible)return;const step=Number(visible.target.dataset.step);cards.forEach(c=>c.classList.toggle('active',c===visible.target));fill.style.width=`${step*20}%`;label.textContent=`${step} of 5`},{rootMargin:'-35% 0px -45% 0px',threshold:[0,.2,.5,.8]});cards.forEach(c=>observer.observe(c));const boxes=[...document.querySelectorAll('#checklist input')];const key='warwick-ready-checklist';try{const saved=JSON.parse(localStorage.getItem(key)||'[]');boxes.forEach((b,i)=>b.checked=!!saved[i]);boxes.forEach((b,i)=>b.addEventListener('change',()=>{localStorage.setItem(key,JSON.stringify(boxes.map(x=>x.checked)))}));document.getElementById('resetChecklist').addEventListener('click',()=>{boxes.forEach(b=>b.checked=false);localStorage.removeItem(key)})}catch(e){}